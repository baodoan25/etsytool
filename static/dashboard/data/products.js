// Deprecated: static mock data has been removed.
// The dashboard now loads listing summaries and detail payloads via API services and custom hooks.

export const products = [];
export const categories = [];
